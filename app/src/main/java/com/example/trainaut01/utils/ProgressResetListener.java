package com.example.trainaut01.utils;

/**
 * Интерфейс для обработки событий сброса прогресса.
 */
public interface ProgressResetListener {

    /**
     * Вызывается при сбросе прогресса.
     */
    void onProgressReset();
}
